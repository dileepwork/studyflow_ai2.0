{
    "name": "studyflowai-root",
    "private": true,
    "scripts": {
        "install": "cd frontend && npm install",
        "vercel-build": "cd frontend && npm install && npm run build && cd .. && rm -rf dist && mv frontend/dist dist",
        "build": "cd frontend && npm install && npm run build && cd .. && rm -rf dist && mv frontend/dist dist"
    }
}