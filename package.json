{
    "name": "studyflowai-root",
    "private": true,
    "scripts": {
        "install": "cd frontend && npm install",
        "vercel-build": "cd frontend && npm install && npm run build && cd .. && mkdir -p dist && cp -rv frontend/dist/. dist/ && ls -la dist",
        "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p dist && cp -rv frontend/dist/. dist/ && ls -la dist"
    }
}